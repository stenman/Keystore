CREATE SCHEMA KEYSTORE;

DROP TABLE KEYSTORE.credentials
IF EXISTS;

CREATE TABLE KEYSTORE.credentials (
  user_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user    VARCHAR(100) NOT NULL,
  password INTEGER NOT NULL
);